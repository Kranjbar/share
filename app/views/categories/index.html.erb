<div style="padding: 5%">
  <h2>Categories</h2>
  <% @categories.each do |category| %>
    <p>
      <strong> <%=link_to "#{category.name}", category_path(category.slug) %> </strong><br>
    </p>
  <% end %>
</div>