<div style="padding: 5%">
  <h2>Please Select a Skill to Add</h2>
  <% @categories.each do |category| %>
    <h4><%=link_to "#{category.name}", category_path(category.id) %><br></h4>
    <% category.skills.each do |skill| %>
			<p><%= skill.name %></p>
		<% end %>
  <% end %>
</div>